import React from 'react';

/**
 * A character that matches what was created in the character builder.
 */
const MatchingCharacter = ({ value }) => {
    return <div className="matching-character">{value}</div>;
};

export default MatchingCharacter;
